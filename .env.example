# URL pública do site, sem barra final. Troque pelo domínio HTTPS antes do deploy.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Contact form (server-side configuration; never use NEXT_PUBLIC_).
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
